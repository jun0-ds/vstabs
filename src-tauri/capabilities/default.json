{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window — vstabs invokes its own commands only.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
